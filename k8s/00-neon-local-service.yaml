apiVersion: v1
kind: Service
metadata:
  name: neon-local
  labels:
    app: neon-local
spec:
  selector:
    app: neon-local
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
